const SectionHeader = ({ title }) => {
  return (
    <div className="flex items-center justify-center">
      <h1 className="text-secondary text-[35px] font-bold"> {title}</h1>
    </div>
  );
};

export default SectionHeader;
